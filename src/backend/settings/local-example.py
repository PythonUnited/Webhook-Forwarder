# This settings file is imported from dev.py
# use this file to store settings which are specific to your local env

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": "sqlite",
    }
}
